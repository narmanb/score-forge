# QOL phase note

The composer toolbar refactor is the first Score Forge quality-of-life slice after WAV export. It deliberately keeps the existing button visual style for the first phone test. Measure copy/paste/duplicate will plug into the same Measure category in a later slice; linked patterns are deferred.
